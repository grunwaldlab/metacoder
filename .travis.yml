language: r
r:
  - release
  - dev

sudo: false
cran: http://cran.at.r-project.org

cache: packages

os:
- linux

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr);codecov()'
  
notifications:
  email:
    on_success: change
    on_failure: always